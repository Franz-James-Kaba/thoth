# Test Case Design Workflow Configuration
name: "test-case-design"
description: "Design detailed test cases and test scenarios with comprehensive coverage"
author: "Franzy"

# Critical variables load from config_source
config_source: "{project-root}/bmad/qa/config.yaml"
output_folder: "{config_source}:output_folder"
test_cases_folder: "{config_source}:test_cases_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated

# Module path and component files
installed_path: "{project-root}/bmad/qa/workflows/test-case-design"
template: "{installed_path}/template.md"
instructions: "{installed_path}/instructions.md"
validation: "{installed_path}/checklist.md"

# Output configuration
default_output_file: "{test_cases_folder}/{feature_name}-test-cases.md"

# Invocation control
standalone: true
